import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  avoidingContainer: {
    flex: 1,
  },
  container: {
    flexGrow: 1,
    justifyContent: 'center',
    padding: 16,
    gap: 10,
    backgroundColor: '#fff',
  },
  header: {
    textAlign: 'center',
    marginBottom: 20,
  },
  formContainer: {
    gap: 10,
  },
  registerBtn: {
    marginTop: 40,
  },
});
